# 국가법령정보센터 OPEN API 키 (선택사항)
# API 키가 없어도 일부 기능을 사용할 수 있지만, 
# API 키가 있으면 더 많은 기능과 안정적인 서비스를 이용할 수 있습니다.
# 발급: https://open.law.go.kr
LAW_API_KEY=your_api_key_here

# 국가법령정보센터 DRF 요청 scheme (http 또는 https)
# 일반 환경 호환성을 위해 기본값은 https 로 두되,
# 제한된 기업망/방화벽 환경에서는 http 가 필요할 수 있습니다.
LAW_GO_KR_DRF_SCHEME=https

# 서버 포트 (기본값: 9099). Windows는 8042–8141 등 예약 구간에 8099가 걸릴 수 있어 기본을 9099로 둠
PORT=9099

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 개발 모드에서 코드 변경 시 자동 재시작 (true/false)
RELOAD=true


# MCP 응답 1회당 최대 크기(바이트). 기본 120000 (한글 약 4만 자).
# 키우면 한 번의 호출이 대화 컨텍스트를 그만큼 더 차지한다.
LEXGUARD_MAX_RESPONSE_BYTES=120000
